const { When } = require("cucumber");
When(
  "a {actor} Books an Available Time Slot on the {actor}'s {permission} Schedule Link",
  function (actor, actor2, permission) {
    // Write code here that turns the phrase above into concrete actions
    return "pending";
  }
);
